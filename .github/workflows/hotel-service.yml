name: 🚀 Hotel Service CI/CD

on:
  push:
    branches: [ "main" ]
    paths:
      - 'backend/services/hotel-service/**'
      - 'shared/**'
      - '.github/workflows/hotel-service.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'backend/services/hotel-service/**'
      - 'shared/**'
      - '.github/workflows/hotel-service.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/reusable-microservice-build-deploy.yml
    with:
      service-name: hotel-service
      working-directory: backend/services/hotel-service
      dockerfile-path: backend/services/hotel-service/Dockerfile
      config-repo-config-file: environments/staging/staging-hotel-service.yaml
      helm-value-path: hotel-service.image.tag
      repo-image-name: phoenix-hotel-service
      target-env: staging
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
      HARBOR_URL: ${{ secrets.HARBOR_URL }}
      HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
      HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
